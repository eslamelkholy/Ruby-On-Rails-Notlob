<%= form_with(model: order_friend, local: true) do |form| %>
  <% if order_friend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_friend.errors.count, "error") %> prohibited this order_friend from being saved:</h2>

      <ul>
        <% order_friend.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.number_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
